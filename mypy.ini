[mypy]
# Ignore third-party stubs we don't control to focus on our code
ignore_missing_imports = True
# Enable stricter checks for our codebase
strict = True
allow_untyped_decorators = True
# Exclude tests from strict checking for now
exclude = tests

# Per-module overrides can be added later for gradual typing
